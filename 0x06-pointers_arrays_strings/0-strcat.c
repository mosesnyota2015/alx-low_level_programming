#include "main.h"
#include <stdio.h>

/**
 * _strcat -  concatenates two strings.
 * Description - The program takes two inputs
 * @dest: destination pointer
 * @src: source pointer
 * Return: The function returns char
 */
char *_strcat(char *dest, char *src)
{
return (strcat(dest, src));
}
