#include "main.h"
/**
 * main - prints if a num is positive , negative or zero
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
        int i;

        i = 0;
        positive_or_negative(i);

        return (0);
}